import React from "react";

export const WeeklySchedule = () => {
    return (
        <section className="schedule-component-container">
            <p>Weekly schedule which will map through the items returned from the database and passed into ScheduleTask.js</p>
        </section>
    );
};